name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-12

    name: Build
    steps:
    - uses: actions/checkout@v3
    #- name: Build
    #  run: xcodebuild -scheme ContributionGraph -project ContributionGraph.xcodeproj
    - name: Run unit tests
      run: xcodebuild test -scheme ContributionGraph -project ContributionGraph.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 11,OS=15.5'
